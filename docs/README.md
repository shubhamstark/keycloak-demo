# docs

- `ARCHITECTURE.md` the components and how each maps to the OAuth / OIDC concepts.
- `AUTH-FLOWS.md` the canonical on-the-wire description of all four flows. Start
  here to understand what the demo proves.
- `MOBILE.md` web vs mobile auth: sessions, refresh rotation, localStorage, and
  how `login-mobile` simulates native app patterns.
- `RUNNING.md` full minikube bring-up walkthrough.
- `NOT-FOR-PRODUCTION.md` the corners this demo cuts on purpose.
